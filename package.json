{
  "scripts": {
    "start": "json-server --watch blog.json --routes routes.json",
    "build": "npm run build:prod",
    "build:prod": "json-server --watch blog.json --port 8080 --production"
  },
  "dependencies": {
    "json-server": "^0.17.1"
  }
}
